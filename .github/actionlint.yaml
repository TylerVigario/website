# actionlint config.
#
# Empty of runner-label declarations on purpose. The `fedora` self-hosted
# label was declared here for the release job, which ran rpmbuild on the
# prod host. That job and the packaging tree are retired, so every job
# now runs on a hosted runner and actionlint needs nothing declared.
#
# When a release shape returns, whatever labels it targets are declared
# here — actionlint flags an undeclared self-hosted label as unknown.
{}
