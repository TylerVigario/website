# actionlint config.
#
# Empty of runner-label declarations on purpose. A `fedora` self-hosted
# label was declared here when the release built on the same machine it
# deployed to. It no longer does — every job, release included, runs on a
# hosted runner — so there is nothing to declare.
#
# If a job ever targets a self-hosted runner again, its labels go here:
# actionlint flags an undeclared self-hosted label as unknown.
{}
